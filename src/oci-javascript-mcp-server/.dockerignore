*

# Container builds only require the dependency manifests and TypeScript sources.
# This excludes local dependencies, tests, coverage, documentation, and VCS files.
!package.json
!package-lock.json
!src/
