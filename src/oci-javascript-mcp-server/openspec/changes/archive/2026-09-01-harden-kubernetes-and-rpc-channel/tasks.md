## 1. Bounded Frame and Channel Foundations

- [x] 1.1 Replace whole-buffer partial-frame concatenation with segmented, single-copy frame assembly and lazy one-message-at-a-time delivery; verify `test/protocol.test.ts` covers split/coalesced frames, byte-at-a-time high-fragment-count input, maximum-frame queued-byte bounds, truncation, invalid UTF-8/JSON, unknown versions, dangerous keys, structural limits, and unchanged 2 MiB rejection.
- [x] 1.2 Define frozen per-execution channel limits derived from the existing OCI call, frame, result, and log limits, pass them from the trusted sandbox coordinator through both providers, and verify focused sandbox/provider tests prove guest code and environment input cannot raise them and configured OCI call/concurrency behavior is preserved.
- [x] 1.3 Replace string-only `appendCapped` use on the trusted channel with a byte-tracking capped accumulator and verify a 1 MiB log followed by a large tiny-log flood performs no further retained-string copies, remains byte-correct for multibyte UTF-8, and advances the cumulative log budget.
- [x] 1.4 Enforce the host-owned configured result limit again when the hostile channel accepts a terminal message, before transitioning to `TERMINAL` or publishing the result; verify raw-channel tests accept payloads below and exactly at a deliberately small limit, reject encoded `result` and `error` payloads above it with the sanitized protocol-failure shape, invoke no OCI work, and begin bounded teardown without relying on the cooperative worker check.
- [x] 1.5 Apply the configured result limit once to the sum of the encoded terminal `result` and `error` byte lengths in both hostile-channel acceptance and outer result validation; verify a raw terminal message whose two values are individually below a 100-byte limit but jointly exceed it is rejected with the sanitized protocol-failure shape, while below-limit and exact-limit combined values remain accepted.

## 2. Ordered Hostile-Channel Protocol

- [x] 2.1 Implement the trusted `WAIT_HEALTH -> RUNNING -> TERMINAL` acceptance state machine with positive safe, execution-unique RPC IDs and synchronous terminal authority revocation; verify channel tests reject pre-health log/RPC/result, duplicate health/result/IDs, unsafe IDs, same-buffer result-plus-RPC/malformed traffic, and post-terminal replay without invoking extra host RPC.
- [x] 2.2 Preserve concurrent accepted OCI RPC completion behind the ordered acceptance layer and route every `execute`, `rpc_result`, and `cancel` frame through one cumulative-budgeted writer; verify the existing four-call concurrency case still reaches four while rejected/flood traffic cannot exceed the message or egress budget.
- [x] 2.3 Honor writable backpressure by pausing input and racing `drain` against close, error, cancellation, and the absolute deadline; verify a stalled fake consumer keeps `writableLength` bounded, emits no later writes before drain, returns the timeout shape by deadline, and completes teardown.
- [x] 2.4 Add sustained valid RPC/log/rejected-request flood tests, large-log-plus-tiny-log tests, and concurrent-execution pressure tests; verify heap-facing queues and pending handlers stay within explicit test bounds and one hostile execution does not alter another execution's result, budgets, deadline, or cleanup.
- [x] 2.5 Re-run the bundled worker exchange plus raw-channel malformed, unknown-version/type/field, missing-field, oversized, invalid-UTF-8, dangerous-key, recursive-limit, non-finite, and truncated-frame coverage through Podman and fake Kubernetes seams; verify the MCP `result`, `error`, `stdout`, `stderr`, `exit_code`, and `timed_out` contract and sanitized OCI/public errors remain unchanged for conforming executions.

## 3. Deadline-Aware Kubernetes Exec and Cleanup

- [x] 3.1 Extend Kubernetes exec establishment with the absolute execution deadline and abort signal, configure a bounded WebSocket handshake, race the SDK promise, and close any late channel before it can enter `RUNNING`; verify Kubernetes API tests cover already-aborted, never-resolving, late-resolving, handshake-error, and successful connections without exposing endpoint details.
- [x] 3.2 Make worker-channel stop accept an absolute cleanup deadline and bound Kubernetes WebSocket/stream closure while preserving idempotent Podman child-tree cleanup; verify a WebSocket that never emits `closed`, repeated stop calls, stream errors, and normal close all settle with the expected sanitized internal outcome.
- [x] 3.3 Refactor Kubernetes termination so exec stop and zero-grace delete/confirmation consume the same cleanup deadline concurrently after pod creation settles; verify deletion is attempted and confirmed when exec setup is pending, a channel arrives late, channel stop rejects, or close never settles.
- [x] 3.4 Verify Kubernetes cleanup failure precedence and deadline authority end to end: confirmed deletion plus unconfirmed channel closure and unconfirmed deletion SHALL return `isolation provider cleanup failed`, while a timed-out execution with confirmed cleanup retains the exact timeout fields and never gains a second cleanup/RPC-drain tail.
- [x] 3.5 Propagate the execution signal and absolute deadline through `createPod` and the client-node create adapter, stop awaiting creation at the cleanup bound, delete immediately by the known pod name, and attach a settlement handler that treats either resolution or rejection after termination begins as an ambiguous outcome requiring a fresh bounded zero-grace delete/confirmation after primary cleanup; verify cancellation-aware never-settling creation returns within the lifecycle bound and observes abort, while signal-ignoring late success and server-created/client-rejected outcomes trigger deletion without watch, exec, worker startup, restored RPC authority, or result replacement.

## 4. Resilient Kubernetes Reconciliation

- [x] 4.1 Return a reconciliation summary with deleted names for internal verification and an aggregate failure count, apply a five-second bound to each candidate, and continue after delete or confirmation failure; verify a persistently failing first pod does not block later expired pods and unrelated, malformed, other-profile, and non-expired pods remain untouched.
- [x] 4.2 Update startup preflight, periodic host reconciliation, and the cleanup-only reconciler to consume the full summary, fail or emit one aggregate sanitized diagnostic as appropriate, and continue future intervals; verify tests expose neither pod names nor raw API errors and retain the cleanup identity's get/list/watch/delete-only authority.
- [x] 4.3 Create one abort signal per reconciliation candidate and propagate it through Kubernetes delete, existence-read, and deletion-watch operations, including client-node generated HTTP request contexts; verify cancellation-aware never-settling fakes observe abort before the next candidate begins and repeated passes do not accumulate active requests from timed-out candidates.

## 5. Exact Kubernetes Admission and Authorization Evidence

- [x] 5.1 Replace anonymous admission mutations with stable identified variants covering every reviewed metadata, profile, image, service-account, token, service-link, restart, host namespace, command/environment, container cardinality, root/security-context, capability, seccomp, filesystem, volume, resource, deadline, port/device/probe/hook, and `/tmp` invariant plus the Kata RuntimeClass; verify exact variant-set tests and immutability tests fail if an invariant is omitted or a mutation weakens the source pod.
- [x] 5.2 Update standard and Kata admission fixture tests so the conforming pod is accepted and every identified variant is rejected, then rename the provider descriptor value from `enforced` to `reviewed-variants-rejected`, retain local `unverified`, and add unverified exact-policy-revision evidence; verify profile/diagnostic tests contain no broader admission or production claim.
- [x] 5.3 Add optional object `name` to Kubernetes self-access attributes, use the configured execution Namespace and Kata RuntimeClass names for cluster-scoped `get`, and leave dynamic pod lifecycle rules namespace-scoped; verify exact SelfSubjectAccessReview payload tests cover both names and all existing lifecycle permissions remain fail-closed.
- [x] 5.4 Add matching Namespace and RuntimeClass `resourceNames` restrictions to standard and Kata example ClusterRoles and verify static manifest/RBAC tests reject missing, wrong, or overbroad cluster object rules while preserving required pod and cleanup reconciler authority.
- [x] 5.5 Replace fixed resource literals in the standard and Kata admission policies with CEL `quantity()` bounds matching the documented CPU, memory, ephemeral-storage, and memory-backed `/tmp` ranges while retaining equal resource requests and limits; replace the now-valid `32Mi` `/tmp` negative variant, add unequal and out-of-range resource variants, and verify default, non-default, minimum, and maximum conforming configurations for both profiles are accepted while malformed, unequal, and out-of-range values are rejected.
- [x] 5.6 Replace relational operators between CEL quantity values with inclusive `quantity(value).compareTo(quantity(min)) >= 0` and `quantity(value).compareTo(quantity(max)) <= 0` checks in both standard and Kata policies; verify static fixtures retain every documented boundary and, when a configured real API server is available, both applied policies report no `status.typeChecking.expressionWarnings`, otherwise record the deliberate environment-only skip without claiming server-side CEL evidence.

## 6. Operator Documentation and Evidence

- [x] 6.1 Replace the nonexistent Kata `npm run kubectl:dry-run:kata` instruction with `npm run kubectl:dry-run:kubernetes` and add a documentation-command test that verifies every Kubernetes-guide `npm run` validation command resolves to a package script.
- [x] 6.2 Update README and Kubernetes/Kata architecture guides for cumulative hostile-channel bounds, strict health/terminal phases, concurrent channel-stop/pod-delete cleanup, aggregate reconciliation behavior, exact-object RBAC, and the narrower admission evidence label; verify the docs retain container-versus-VM/Kata POC distinctions, no-fallback behavior, rollback/orphan recovery guidance, and contain no credentials, tenancy values, or local absolute paths.
- [x] 6.3 Run the opt-in local-cluster lifecycle harness when explicit kubeconfig test inputs are available and verify create/watch/exec/result, timeout/cancellation, deletion, and reconciliation under the new channel lifecycle; otherwise record the deliberate skip and do not claim real admission, CNI, CRI, or Kata evidence.
- [x] 6.4 Update operator documentation to state that every documented reviewed Kubernetes resource value is accepted by the example policies without synchronized policy edits and to describe request/limit equality plus CPU, memory, ephemeral-storage, and `/tmp` range enforcement; retain the explicit distinction between offline fixtures and real-cluster admission evidence.
- [x] 6.5 Complete the standard in-cluster manifest bundle with the hardened trusted-host Deployment, host-only OCI credential Secret contract, immutable host/runner image placeholders, and downward-API host identity; verify manifest tests require runner-digest alignment with admission and prohibit credential material in example assets.
- [x] 6.6 Complete the Kata in-cluster manifest bundle with a hardened trusted-host Deployment using the exact Kata RuntimeClass and handler, host-only OCI credential Secret contract, immutable host/runner image placeholders, and downward-API host identity; verify manifest tests and the Kata guide require image/admission alignment and preserve the POC-only assurance boundary.

## 7. Integrated Validation and Handoff

- [x] 7.1 Run focused protocol, worker-channel, sandbox, Kubernetes API/provider/profile/reconciler, manifest, and MCP stdio tests; verify both providers preserve supported results and host credential/SDK request/client-option/call/concurrency/public-error controls while all new adversarial cases pass deterministically.
- [x] 7.2 Run `npm test`, `npm run coverage`, `npm run check`, `npm run check:kubernetes-manifests`, `npm run kubectl:dry-run:kubernetes`, `npm run packcheck`, and `npm run ci`; verify every applicable command passes, coverage remains at least 90%, and any environment-only skip or scoped elevated dry-run retry is reported exactly.
- [x] 7.3 Verify the implementation diff is self-contained in this server, no generated/local artifacts or unrelated findings were changed, no new runtime dependency was added, no changelog is required because this server has none, and `git diff --check` plus `openspec validate harden-kubernetes-and-rpc-channel --strict` pass before review.
- [x] 7.4 Re-run focused hostile-channel, Kubernetes API/reconciler, admission-variant, manifest, profile, and MCP compatibility tests plus `npm run check`, `npm run coverage`, `npm run check:kubernetes-manifests`, `npm run kubectl:dry-run:kubernetes`, `npm run packcheck`, `npm run ci`, `git diff --check`, and strict OpenSpec validation; run the opt-in real-cluster admission path when configured, otherwise record the deliberate skip and do not claim server-side CEL or runtime evidence.
- [x] 7.5 Re-run the combined-terminal-budget, never/late/ambiguous pod-creation, and CEL comparison regressions plus focused hostile-channel, Kubernetes API/provider, manifest, profile, and MCP compatibility tests; then run `npm test`, `npm run coverage`, `npm run check`, `npm run check:kubernetes-manifests`, `npm run kubectl:dry-run:kubernetes`, `npm run packcheck`, `npm run ci`, `git diff --check`, and `openspec validate harden-kubernetes-and-rpc-channel --strict`, verifying at least 90% line coverage and reporting any real-cluster-only skip exactly.
