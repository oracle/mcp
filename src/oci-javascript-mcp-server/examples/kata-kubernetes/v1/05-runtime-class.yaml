apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata-qemu-runtime-rs
handler: kata-qemu-runtime-rs
# Scheduling, tolerations, and overhead must be filled from reviewed real-node
# evidence for the admitted Kata/CRI-O deployment profile.
