## 1. Provider Configuration and Dependencies

- [x] 1.1 Add the official Kubernetes JavaScript client to `package.json` and `package-lock.json`, review its install scripts and transitive dependency diff, and verify `npm install` plus `npm run packcheck` succeed without unapproved package contents.
- [x] 1.2 Implement strict parsing for `OCI_JAVASCRIPT_ISOLATION_PROVIDER` and all required Kata configuration, including the execution namespace, downward-API trusted-host namespace/name/UID, DNS-safe identifiers, bounded numeric settings, and a required `@sha256` runner digest; verify focused tests reject missing, unknown, unsafe, mutable, out-of-range, and same-namespace values.
- [x] 1.3 Add a provider factory that retains Podman as the unset default, selects Kata only when explicitly configured, and never falls back; verify unit tests cover default, both explicit providers, invalid configuration, and construction failure.
- [x] 1.4 Construct and preflight the selected provider before connecting MCP stdio in `src/server.ts`; verify an MCP subprocess test proves invalid startup configuration exits without accepting a tool request or launching any provider.

## 2. Provider-Neutral Framed Channel

- [x] 2.1 Define a minimal internal worker-channel contract for readable output, writable input, close/status, and idempotent stop, then refactor `src/isolation/pipe-execution.ts` to consume it; verify existing framed execution tests pass without protocol schema changes.
- [x] 2.2 Add a child-process channel adapter for Podman and preserve its exact hardened launch, environment allowlist, result limits, cancellation, and named cleanup behavior; verify the fake-Podman argument and end-to-end sandbox tests pass unchanged except for intentional adapter seams.
- [x] 2.3 Add channel-level tests for close-before-health, duplicate health, writes after close, child/stream errors, result-versus-close races, and idempotent stop; verify each case settles once and leaves no pending timer or promise.
- [x] 2.4 Run the existing malformed JSON, invalid UTF-8, unknown version/type/field, missing field, oversized frame, dangerous key, structural limit, non-finite number, and truncated-frame tests through the generic channel; verify no rejected input reaches host OCI RPC.

## 3. Kata Startup Preflight and Trusted Diagnostics

- [x] 3.1 Add injectable Kubernetes API and exec-stream interfaces around in-cluster client loading so production uses only in-cluster credentials and tests use deterministic fakes; verify tests prove no kubeconfig fallback or guest-visible Kubernetes credential is introduced.
- [x] 3.2 Implement startup reads for the execution Namespace, RuntimeClass, and handler plus validation of the downward-API trusted-host namespace/name/UID while treating the node CRI mapping as external evidence; verify same-namespace configuration, mismatches, and NotFound failures prevent provider construction before any execution pod is created and tests prove the provider does not read or modify containerd, CRI-O, node files, or runtime sockets.
- [x] 3.3 Implement SelfSubjectAccessReview preflight for the exact cross-namespace pod, pod-exec, watch/list/delete, RuntimeClass-read, and host reconciliation permissions; verify fake authorization responses fail closed for every missing required verb and prove the execution RoleBinding grants no authority in the trusted-host namespace.
- [x] 3.4 Implement fail-closed non-persistent admission probes for the conforming pod plus reviewed unsafe variants covering mutable image, wrong RuntimeClass/service account, token mounting, unexpected command/environment, host access, extra volumes, and weakened security context; verify fake and integration tests fail startup when the conforming shape is rejected or any unsafe variant is accepted and leave no probe pod behind.
- [x] 3.5 Add the trusted provider descriptor and bounded stderr/telemetry event schema with allowlisted phases, outcomes, reason codes, durations, correlation IDs, configured RuntimeClass/handler identity, namespace separation, admission outcome, and explicit unverified external-control flags for CRI, node, CNI, PID, overhead, and image evidence; verify tests prove code, protocol payloads, guest output, raw Kubernetes errors, credentials, endpoints, and resource details are excluded.

## 4. Hardened Kata Pod Construction

- [x] 4.1 Implement a pure execution-pod builder using the configured RuntimeClass, runtime handler preflight, digest-pinned image, trusted-host namespace/name/UID, correlation and expiry metadata, no owner reference, `Never` restart policy, active deadline, and fixed silent Node wait command; verify an exact-object test rejects owner references and any mutable or operator-supplied arbitrary pod fragment.
- [x] 4.2 Add the fixed non-root UID/GID, `runAsNonRoot`, no privilege escalation, dropped capabilities, `RuntimeDefault` seccomp, read-only root filesystem, no host namespaces, disabled service links and token automount, and the zero-authority runner service account; verify security-context tests assert every required field and absence of sidecars, ports, devices, host paths, and unexpected environment entries.
- [x] 4.3 Add equal CPU/memory requests and limits, bounded ephemeral storage, and the sole size-limited memory-backed `/tmp` volume; verify tests assert the reviewed defaults, clamps, mount flags, and absence of all other writable volumes.
- [x] 4.4 Derive pod active deadline and expiry from the host absolute deadline plus bounded cleanup allowance; verify boundary tests cover sub-second remaining time, the 120-second maximum execution, rounding, expired-before-create, and overflow/invalid inputs.

## 5. Kubernetes Execution Lifecycle

- [x] 5.1 Implement the asynchronous Kata execution state machine for create, pending watch, running, exec connect, execution, close, delete, and deleted phases while preserving the synchronous `IsolationProvider.run()` handle; verify fake API tests cover the successful phase order and one fresh pod per call.
- [x] 5.2 Start the fixed sandbox-worker command through a non-TTY Kubernetes exec stream only after the pod is Running, adapt WebSocket stdin/stdout/status to the generic channel, and cap or discard raw exec stderr; verify a fake exec test completes a real framed health/execute/RPC/result exchange with no readiness race or raw stderr disclosure.
- [x] 5.3 Map pod scheduling failure, image pull failure, container termination, API failure, exec failure, and WebSocket loss to the existing sanitized provider result; verify each test returns only `isolation provider failed` unless the absolute deadline requires the timeout result.
- [x] 5.4 Make abort and timeout authoritative during create, pending, connecting, and executing phases, attempt protocol cancellation after connection, and always proceed to forced deletion; verify race tests cover abort-before-create-response, late watch/exec completion, unacknowledged cancellation, and timeout-result shape.
- [x] 5.5 Extend `IsolationExecution` with a host-clamped termination budget, retain Podman's six-second allowance, and permit Kata up to the reviewed 30-second default/60-second maximum; verify tests reject provider values outside the host clamp and preserve cleanup-failure precedence.
- [x] 5.6 Implement idempotent zero-grace pod deletion, exec-channel closure, and NotFound confirmation; verify success is returned only after confirmed deletion and unconfirmed deletion returns the exact `isolation provider cleanup failed` result.
- [x] 5.7 Add startup and periodic host-side expiry reconciliation for labeled managed pods without owner references; verify multi-replica tests delete expired pods, preserve every non-expired pod including those created by another host, ignore unrelated pods, and settle concurrent reconciliation/normal cleanup safely.
- [x] 5.8 Implement a cleanup-only reconciler entry point using a separate identity and only get/list/watch/delete behavior for expired managed pods; verify tests cover all host replicas being unavailable, reconciler restart and outage recovery, malformed or missing expiry metadata, unrelated/non-expired preservation, overlapping deletion with normal cleanup, and bounded sanitized diagnostics.

## 6. Security and Compatibility Test Coverage

- [x] 6.1 Add provider-selection MCP stdio integration tests proving identical `run_javascript` success, script-error, log, OCI RPC, timeout, and result fields for Podman and a fake Kata provider; verify provider-specific control-plane details never enter MCP structured content.
- [x] 6.2 Add adversarial fake-runner tests that bypass the facade and send direct raw frames, invalid sequences, unsupported client options, oversized requests/results, excessive calls, and concurrent RPC floods; verify the existing host validation and budgets remain authoritative through Kata.
- [x] 6.3 Add credential and authority regression tests proving Kata pod configuration contains no OCI/Kubernetes credential, host mount, or owner reference; prove the cross-namespace execution grant cannot reach trusted-host pods and provider selection does not broaden endpoints, signers, retry configuration, SDK operations, deadlines, call budgets, or response sanitization.
- [x] 6.4 Add deterministic correctness tests using a small fixed number of simultaneous Kata executions, unique names/channels, result isolation, independent cancellation, configured concurrency-limit behavior, host/independent reconciliation overlap, and cleanup; verify no fake pod, stream, timer, watcher, reconciler operation, or pending RPC leaks after the test, without establishing load or throughput targets.
- [x] 6.5 Keep all new source under the configured c8 scope and add focused tests until `npm run coverage` passes the repository's 90% line threshold without excluding the new provider or lifecycle code.

## 7. Kubernetes Deployment Assets and Documentation

- [x] 7.1 Add versioned example manifests for distinct trusted-host and execution namespaces, restricted execution Pod Security labels, trusted host and zero-authority runner service accounts, the cross-namespace host RoleBinding, cleanup-only reconciler service account and RoleBinding, ResourceQuota/LimitRange, and default-deny execution ingress/egress NetworkPolicies; verify manifest schema/static tests and `kubectl apply --dry-run=client` accept every asset, and a focused assertion proves the execution grants name only the host service account while the guest receives no token or privileged authority.
- [x] 7.2 Add versioned fail-closed admission policy and binding assets that constrain host-created execution pods to the reviewed shape plus a cleanup-only reconciler deployment outside the execution namespace; verify offline fixtures or policy tests identify the conforming shape and every reviewed unsafe variant, and static RBAC tests prove the reconciler has no create or `pods/exec` authority. Record server-side dry-run enforcement as deferred real-cluster evidence.
- [x] 7.3 Document a reproducible runner-image build, immutable-digest configuration flow, CRI-neutral provider boundary, required Kata installation/RuntimeClass mapping, and future OKE CRI-O validation profile; verify the guide includes separate namespaces, excludes OKE virtual nodes, explains that `containerd-shim-kata-v2` does not require containerd as the node CRI, and clearly labels live cluster evidence as deferred rather than POC acceptance.
- [x] 7.4 Document all provider environment variables, in-cluster-only topology, startup/admission failure behavior, end-to-end timeout semantics, cleanup tail, conservative concurrency defaults, operator diagnostics, no-fallback behavior, and local Podman compatibility; verify examples contain no tenancy-specific values, credentials, or local absolute paths and do not claim high-concurrency or production readiness.
- [x] 7.5 Document future deployment, canary, orphan inspection/cleanup, reconciler recovery, rollback, node maintenance, and revalidation steps as non-blocking guidance; verify the guide identifies the real-provider and security-review evidence still required before production admission and never presents Podman as a production-equivalent VM boundary.

## 8. Final POC Validation and Handoff

- [x] 8.1 Run `npm test`, `npm run coverage`, `npm run check`, and `npm run packcheck`, then run `npm run ci`; verify every command passes on the documented Node/npm versions and coverage remains at or above 90% for the new provider and lifecycle code.
- [x] 8.2 Run the documented offline manifest, admission-fixture, and static RBAC checks; verify all example assets are syntactically valid, trusted-host and execution namespaces remain distinct, and deferred server-side or real-cluster assertions are explicitly identified.
- [x] 8.3 Verify the implementation diff remains self-contained in this server, generated/local coverage artifacts are unmodified, changelog handling follows repository policy, and `git diff --check` reports no whitespace errors.
- [x] 8.4 Re-run `openspec validate add-kata-kubernetes-isolation-provider --strict` after implementation and verify the specs, design decisions, completed task evidence, README, and observed POC behavior remain coherent before requesting review.

## 9. Kubernetes Engine and Profile Refactor

- [x] 9.1 Replace the interim `podman`/`kata-kubernetes` selection with `podman`/`kubernetes` plus exact `local-development`, `in-cluster`, and `kata-in-cluster` profile parsing; verify focused tests reject missing, unknown, and profile-incompatible configuration without changing the unset Podman default.
- [x] 9.2 Extract the Kata-named Kubernetes API, shared config, pod builder, diagnostics, provider lifecycle, and reconciler into generic Kubernetes modules while keeping runtime-specific fields out of the shared contract; verify all existing lifecycle, race, protocol, deletion, and reconciliation tests pass through the renamed engine.
- [x] 9.3 Define typed monotonic standard and Kata runtime policies that can add reviewed runtime constraints and preflights but cannot mutate base pod security, credentials, commands, environment, volumes, resources, deadlines, or cleanup; verify exact pod tests prove both profiles share the base shape and only Kata adds `runtimeClassName`.
- [x] 9.4 Add explicit kubeconfig and in-cluster connection factories with no credential auto-detection or fallback; verify local configuration requires a path and context, both in-cluster profiles use only `loadFromCluster()`, and no execution pod receives Kubernetes credentials.
- [x] 9.5 Implement profile-aware preflight and descriptors: local lifecycle capability with development-only gaps, standard in-cluster RBAC/admission without RuntimeClass, and Kata in-cluster additive handler/admission/evidence checks; verify every missing required control fails or is reported according to the selected profile's contract.
- [x] 9.6 Implement profile-aware image policy so both in-cluster profiles require a digest while local development permits an explicitly opted-in safe tag only with `imagePullPolicy: Never`; verify mutable in-cluster images fail and local relaxed provenance is reported.
- [x] 9.7 Generalize managed labels, diagnostics, expiry cleanup, and reconciliation across profiles while retaining exact-profile adoption and the cleanup-only in-cluster reconciler; verify expired local and in-cluster pods are deleted without adopting unrelated, malformed, other-profile, or non-expired pods.

## 10. Profile Validation and Deployment Evidence

- [x] 10.1 Add focused configuration, factory, pod, descriptor, and MCP stdio tests for all three Kubernetes profiles and Podman; verify compatible result fields, sanitized failures, no fallback, and unchanged OCI/protocol authority.
- [x] 10.2 Add an opt-in local-cluster lifecycle harness using the explicit kubeconfig profile to exercise namespace access, create, watch, exec, framed health/execute/result exchange, timeout/cancellation, deletion confirmation, and reconciliation; verify it skips clearly when no configured local cluster is available and never claims Kata evidence.
- [x] 10.3 Add standard-runtime in-cluster manifest or overlay assets for separate namespaces, service accounts, least-privilege RBAC, base-pod admission, resource/network controls, and the cleanup reconciler without RuntimeClass requirements; verify static manifest, RBAC, and admission tests distinguish standard from Kata policy.
- [x] 10.4 Retain and adapt Kata-specific tests and deployment assets for exact RuntimeClass/handler validation, wrong-runtime admission rejection, CRI-neutral behavior, OKE/CRI-O evidence, and VM-boundary disclaimers; verify the generic refactor does not weaken any prior Kata assertion.
- [x] 10.5 Update README and deployment guides with the provider/profile matrix, explicit local kubeconfig workflow, standard in-cluster topology, Kata-only evidence, image policies, assurance descriptors, and no-fallback semantics; verify examples contain no credentials, tenancy-specific values, or local absolute paths.

## 11. Revised Change Validation

- [x] 11.1 Run focused profile tests, `npm test`, `npm run coverage`, `npm run check`, `npm run packcheck`, manifest/static checks, and `npm run ci`; verify coverage remains at least 90% and every profile-specific security assertion passes.
- [x] 11.2 Verify the refactor remains self-contained in this server, generated artifacts are untouched, changelog handling follows repository policy, and `git diff --check` passes.
- [x] 11.3 Run `openspec validate add-kata-kubernetes-isolation-provider --strict` and verify proposal, both capability specs, design, reopened tasks, documentation, implementation, and observed profile behavior are coherent before sync or archive.
